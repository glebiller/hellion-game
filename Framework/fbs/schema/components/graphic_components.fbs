namespace Schema.Components;

struct GraphicColor {
    red: float;
    green: float;
    blue: float;
    alpha: float;
}

table GraphicAmbientLight {
    color: GraphicColor;
}

table GraphicLight {
    type: string;
    diffuseColor: GraphicColor;
    specularColor: GraphicColor;
}

table GraphicSkybox {
    enabled: bool;
    material: string;
    distance: double;
}

table GraphicTerrain {

}

table GraphicCamera {

}

table GraphicMesh {
    name:string (required);
}